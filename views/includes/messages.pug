.sixteen.wide.column
  if (success.length > 0)
    .ui.success.tiny.message
      i.close.icon
      .header= success.join(' ')
  if (errors.length > 0)
    .ui.error.tiny.message
      i.close.icon
      .header= errors.join(' ')
